import {
    StyleSheet
  } from 'react-native';

const style = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center'
    },
    greeting: {
        marginVertical: 32,
        fontSize: 18,
        fontWeight: '400',
        textAlign: 'center',
    },
    buttonText: {
        color: '#1A73E8',
        fontWeight: '500'
    }
})

export default style;